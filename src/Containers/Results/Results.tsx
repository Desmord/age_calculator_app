import styles from './Results.module.scss';

const Results = ({
    days,
    years,
    months,
}: {
    days: string,
    years: string,
    months: string,
}) => {
    return (
        <div>Results</div>
    )
}

export default Results;